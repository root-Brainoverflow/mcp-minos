# Base URL the frontend uses for API calls. Defaults to "/api" (proxied by
# Vite in dev and nginx in prod). Set to an absolute URL to point elsewhere.
# VITE_API_BASE=/api

# Dev-only: where `vite dev` proxies /api to. Defaults to http://localhost:8000.
# VITE_API_PROXY=http://localhost:8000
